// Civics Quest — Prisma Schema
// Phase 0: datasource + generator only.
// Full schema models are added in Phase 1 (spec Section 27).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
