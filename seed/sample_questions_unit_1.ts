/**
 * Seed: Sample Questions — Unit 1 (SS.7.CG.1.x benchmarks)
 *
 * Phase 0: stub.
 * Phase 1: implement with ≥ 15 questions per Unit 1 benchmark, fully tagged.
 *
 * Required tags per spec Rule 3 and Section 13:
 *   benchmark_id, reporting_category_id, cognitive_complexity (Low/Moderate/High),
 *   stimulus_type (text/graphic/map/chart/primary_source/none),
 *   reading_load_level (1|2|3), skill_tag, misconception_id,
 *   remediation_tag, source_tier (A|B|C|D), approval_status
 *
 * All seed questions: source_tier = "B", approval_status = "approved"
 */

// import { PrismaClient } from '@prisma/client'

export async function seedSampleQuestions(): Promise<void> {
  // TODO: Phase 1 implementation
  console.log('seedSampleQuestions: stub — implement in Phase 1')
}
