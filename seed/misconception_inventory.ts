/**
 * Seed: Misconception Inventory
 *
 * Phase 0: stub.
 * Phase 1: implement with all 50 entries from spec Appendix E.
 *
 * Each entry has:
 *   code (e.g. MC-001), description, reporting_category_id,
 *   remediation_hint, common_distractor_pattern
 */

// import { PrismaClient } from '@prisma/client'

export async function seedMisconceptions(): Promise<void> {
  // TODO: Phase 1 implementation
  console.log('seedMisconceptions: stub — implement in Phase 1')
}
